# Notion Integration token (from https://www.notion.so/profile/integrations)
NOTION_TOKEN=

# Random long secret for HMAC-SHA256 config signing
CONFIG_SECRET=

# Admin key required for /setup and /api/config/sign
ADMIN_KEY=

# Public app URL (used for generating embed URLs)
NEXT_PUBLIC_APP_URL=http://localhost:3000
